<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Material.Components.Controls">

    <Style TargetType="{x:Type controls:FocusIndicator}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Brush" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:FocusIndicator.Brush)}" />
        <Setter Property="Device" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:FocusIndicator.Device)}" />
        <Setter Property="Offset" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:FocusIndicator.Offset)}" />
        <Setter Property="Thickness" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:FocusIndicator.Thickness)}" />
    </Style>
</ResourceDictionary>